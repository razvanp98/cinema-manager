<html>
<head>
    <title>Untitled</title>
</head>
<body>
    <h1> / has been requested.</h1>

    <ul>
        <% data._question.forEach(item => { %>
            <li>Question <%= item.id %> <%= item._body %></li>
        <% }) %>
    </ul>
</body>
</html>